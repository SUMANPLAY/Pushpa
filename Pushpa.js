const videoPlayer = document.getElementById("videoPlayer");

// Prevent right-click to block download options
videoPlayer.addEventListener("contextmenu", (e) => e.preventDefault());

// Additional future customizations
